# Shape A increment-inference dependence firewall

Fold-level t/Wilcoxon tests are forbidden. Folds and repeated cross-validation runs are dependent views of the same at-most-128 `stable_example_id` units, not independent replications. A coarse restricted permutation of `D` cannot preserve `D | P2_raw`; it is labelled artifact sensitivity, never an exact CRT.

The central result is one conjunction, never a menu: (A) partial slope, partial R2, and HC3; (B) the magnitude of one paired OOF-loss difference per example; and (C) structural sham, sensitivity, and paired/stacked algebra audit. A, B, and C cannot be selected based on which is positive. The stacked `arm×D` coefficient has been removed from the maxT family because it duplicates the paired estimand.

The validator preregisters eight false-positive counterexamples: pure difficulty, P2 redundancy, role prevalence, a single outlier, heteroskedasticity alone, fold accident, regularization suppression, and coarse-permutation artifact. The algebra auditor emits neither p-values nor claim authorization.
