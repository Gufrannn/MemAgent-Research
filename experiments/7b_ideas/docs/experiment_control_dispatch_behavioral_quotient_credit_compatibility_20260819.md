# Experiment-control dispatch: BQCC

Do not launch GPU work. Wait for the legitimate corrected 2×2/BOT invariant/necessary controlled-pair ledger and its outcome-pre-frozen canonical manifest. The analyzer hard-checks the canonical SHA, unique pairs, both relation classes, minimum stable examples per class, frozen direction, positive scale, and the pairwise affine identity. Identity uses the maximum pair error; averaging inside an example cannot hide a broken implementation. Metrics average pairs to examples before stable-example cluster bootstrap, and pair/path/turn never increase n.

The only machine outcomes are `FINITE_QUOTIENT_COMPATIBLE`, `NUISANCE_SPECIFICITY_FAILURE`, `REWARD_ALIASING_REGROUPING_CANNOT_REPAIR`, `PARTITION_CROSSING_GROUPING_ONLY_NO_GO`, and `IMPLEMENTATION_IDENTITY_FAILURE` after ledger qualification.

BQCC ends at scalar quotient compatibility. Delivery has the irreducible order `G_credit → G_task → G_total → Delta_theta_actual`. PPO clipping can make local task delivery zero at A=+1,r=1.3 and A=-1,r=.7 with epsilon=.2, while r=1.1 delivers 1.1. Therefore BQCC pass plus silent G_task is a trust-region delivery bottleneck, not reward failure; a large G_total or gradient norm cannot repair BQCC failure. Enter existing CSFGW/W4 only for a claim that the actual parameter update used the credit or to request a repair unlock. Pure diagnosis stops at BQCC.
