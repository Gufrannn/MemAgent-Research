# Experiment-control dispatch: BQCC

Do not launch GPU work. Wait for the legitimate corrected 2×2/BOT invariant/necessary `PAIRS.jsonl`, complete `TARGET_LEDGER.jsonl`, and outcome-pre-frozen `MANIFEST.json`. The analyzer hard-checks all canonical SHA values, target→admitted→analyzed closure, unique pairs, both relation classes, minimum stable examples per class, frozen direction, positive scale, and the pairwise affine identity. Identity uses the maximum pair error; averaging inside an example cannot hide a broken implementation. Metrics average pairs to examples before stable-example cluster bootstrap, and pair/path/turn never increase n. Infrastructure failure blocks a point verdict.

Coverage sufficient to run does not identify the full target population. Unless I/N pair and example coverage are all 100%, or a separately frozen validated missingness/partial-ID model exists, claim scope is `admitted_relation_stratum_only`. Each axis is interval-aware: `LOW_CERTIFIED`, `HIGH_ESTABLISHED`, or `THRESHOLD_UNCERTAIN`. Any uncertain axis returns `BQCC_INCONCLUSIVE_THRESHOLD_UNCERTAINTY` and forbids a named nuisance/aliasing/crossing diagnosis. The sole success label remains `FINITE_QUOTIENT_COMPATIBLE`.

BQCC ends at scalar quotient compatibility. Delivery has the irreducible order `G_credit → G_task → G_total → Delta_theta_actual`. PPO clipping can make local task delivery zero at A=+1,r=1.3 and A=-1,r=.7 with epsilon=.2, while r=1.1 delivers 1.1. Therefore BQCC pass plus silent G_task is a trust-region delivery bottleneck, not reward failure; a large G_total or gradient norm cannot repair BQCC failure. Enter existing CSFGW/W4 only for a claim that the actual parameter update used the credit or to request a repair unlock. Pure diagnosis stops at BQCC.
