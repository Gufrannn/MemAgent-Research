# P33.2-v3.1 H20 handoff: UMA official 4B real rollout credit anatomy

## Critical update

Do not use the old P33.2-v3 commit for H20.

P33.2-v3.1 repairs the primary trajectory identity contract:

- primary trajectory identity: official `trajectory_id` generated by `ToolMemoryAgentLoop`;
- derived `trajectory_key=(step,sample_index,rollout_n,validate)` is provenance / cross-check only;
- `trajectory_id -> trajectory_key` must be exactly one-to-one on the ID side;
- `trajectory_key -> multiple trajectory_id` is allowed and reported;
- reward ranks are tie-aware dense ranks.

## Purpose

Run official UMA 4B real rollout credit trace anatomy and read the real `reward / advantage / trajectory / memory-step` structure.

This is not training, not a method result, and not an RL necessity claim.

## Must not change

- prompt
- memory operator
- split / data protocol
- metric / evaluator
- generation protocol
- reward formula
- trainer objective
- optimizer behavior, unless a one-step dry run is unavoidable and explicitly reported

## Required GitHub input

Use only the exact commit that contains the P33.2-v3.1 summarizer repair.

Current local v3.1 package is ready, but the final GitHub exact commit must be filled after push.

## H20 run target

- 8–16 original samples;
- multiple rollouts;
- multiple memory conversations per trajectory;
- target memory rows ≥ 100;
- if Ledger-QA is easy, use Ledger-QA;
- otherwise use official UMA native training data first to test plumbing.

## Required output

Return compact text with:

- exact commit;
- run root;
- run log;
- trace file count;
- trace event count;
- memory row count;
- identity contract status;
- `n_trajectory_ids`;
- `n_trajectory_keys`;
- `trajectory_keys_with_multiple_trajectory_ids`;
- summary JSON SHA256;
- table CSV SHA256;
- whether optimizer step happened;
- `Var_t(QA | trajectory_id)`;
- `Var_t(advantage | trajectory_id)`;
- trajectory-level mean memory advantage;
- tie-aware reward dense-rank distribution within GRPO groups;
- 3–5 concrete trajectory examples.

## Boundary

P33.2-v3.1 can only establish real credit-anatomy structure. It still cannot prove credit is wrong. P33.3 intervention reference is required for credit-resolution mismatch claims.
